# Приёмка задания 004 — Lab v0.9.3

**Решение:** ПРИНЯТО 2026-09-25 после двух раундов. Исполнитель — GPT-5.6 Sol (agent), API-коннектор без локального git и стенда (контракт §9.4).

## Ход

| Этап | Что было | Итог |
|---|---|---|
| Выдача, редакция 1 | задание предполагало у исполнителя локальный git с сетью и прогон стенда | исполнитель остановился до работы: подпись через `user.name` и самопроверка до push для него невыполнимы; ничего не имитировал, ветку не выдавал за поставку |
| Редакция 2 | tarball'ы поставили мы (`lab/vendor/`, sha512 = integrity lock); оба способа офлайн-установки проверены нами; подпись — строкой `Agent:`; `sums_by: reviewer`; контракт 1.6 §9.4 | — |
| Раунд 1 (`7554fc3`, `31d7096`) | способ A (временный кэш из `vendor/` + `npm ci --offline`), общий модуль `lab/src/engine.js`, фактическая версия движка в отчётах, негативный QA-случай, v0.9.3, документация | все критерии A1–A8 выполнены; 3 замечания |
| Раунд 2 (`281a2cd`, `6f11f6b`) | исправлены все 3 замечания, ничего сверх них | принято |

## Критерии (раунд 1 — полный прогон; раунд 2 — целевой, по затронутым путям)

| # | Критерий | Результат |
|---|---|---|
| A1 | Guard | `BRANCH CHECK: PASS` в обоих раундах; предупреждения только о SUMS (`sums_by: reviewer`) |
| A2 | tarball'ы не тронуты | PASS (`lab/vendor/` защищён, изменений нет) |
| A3 | Установка без сети | PASS: чистый checkout, реестр подменён на мёртвый адрес, `INSTALL.cmd` rc=0; пользовательский кэш не тронут, временный удалён |
| A4 | Движок тот же | PASS: `simulation` `e03e076878187088`, `csv-parse` `eabc34c614f01b34` — совпадают с установкой из реестра |
| A5 | Пин обеспечен | PASS: `simulation` 9.0.1 → `RUN_LAB` и `CHECK_CANDIDATE` отказываются (после раунда 2 — `[ERROR] …`, код 2, без трассировки); с отключённой проверкой новый QA-случай падает (29/30) |
| A6 | Поведение стенда | PASS: QA 30/30, STRUCTURE 21/21, CONFORMANCE 18/18, POLICY 10/10, COMPARE PASS, SELF_TEST PASS; `RUN_LAB` Modes 0–26 `OVERALL: PASS`; `CHECK_CANDIDATE` `BYTE_IDENTICAL` + `POLICY RESULT: PASS`, в отчётах фактическая `simulation 9.0.0` |
| A7 | Документация | PASS: утверждений «npm install» / «зависимости не поставляются» не осталось |
| A8 | Отчёт | PASS: в обоих раундах перечислено, что не запускалось |

## Замечания раунда 1 и их закрытие

1. `lab/src/workspace.js` — литерал `'9.0.0'` в манифесте web-проверки → `EXPECTED_ENGINE_VERSION`; проверено: манифест пишет `simulation 9.0.0` из константы.
2. Отказ пина был необработанным исключением с кодом 1 → `[ERROR] …`, код 2 (соглашение стенда); проверено на `RUN_LAB` и `CHECK_CANDIDATE`.
3. `lab/src/policy_qa.js` — эталонная policy брала фактическую версию → ожидаемую; POLICY QA 10/10.

## Что сделали мы при слиянии

Этот коммит: `ACCEPTANCE_RU.md`, результаты v0.9.3 в `lab/docs/TEST_STATUS_RU.md`, пересборка `SHA256SUMS.txt` и `lab/SHA256SUMS.txt` (`node tools/build_sums.mjs`). Коммиты исполнителя сохранены как есть; `main` — fast-forward.

## Уроки цикла

- **Среду исполнителя выясняем до выдачи.** Одна итерация ушла на задание, невыполнимое в среде агента; теперь это шаг 0 в `docs/tasks/README_RU.md`.
- **Работа вслепую + наш прогон — рабочая схема.** Код, написанный без единого запуска, прошёл стенд с первого раунда; guard и стенд подтвердили каждое заявление отчёта и нашли то, что трассировка кода пропустила (литерал вне путей импорта движка).
- **Цикл доработки работает:** замечание → новые коммиты в ту же ветку → раздел «Раунд N» → целевая перепроверка. Полный прогон стенда (~20 мин, из них `CHECK_CANDIDATE` ~11 мин) нужен только когда затронуты пути симуляции/сравнения.
