# Приёмка задания 009 — v7.7.1 Transport on Construction Materials

**Решение:** ПРИНЯТО 2026-09-26 с первого раунда; продвинуто в accepted **v7.7.1 r1**. Исполнитель — GPT-5.6 Sol (agent), API-коннектор + `candidate.yml`.

## Ход

7 коммитов, КТ1–КТ4, всё в одной сессии. Сообщения в чате снова обрывались по времени доставки, но работа шла: к моменту обрыва сообщения ветка уже содержала КТ1–КТ3. Калибровка — «зонды → пороги», как в 008: три порога с запасом и смысловым обоснованием, константа `Transport Construction Materials per Capacity` = 3 оставлена на значении skeleton.

## Критерии (канонические — Windows)

| # | Критерий | Результат |
|---|---|---|
| E1 | guard (строгий), CI | PASS: 7 коммитов, линейно, `Agent:` в каждом, 5 путей в scope, 4 обязательных файла; `candidate.yml` 5/5 (run `36234471956`), `ci.yml` зелёный (`36234471931`) |
| E2 | полный цикл на Windows | кандидат SHA `a3c37139…` (= Linux); conformance PASS; audit PASS; validation **32/32** (5120 проверок, 0 провалов); `CHECK_CANDIDATE` с policy исполнителя — **`POLICY PASS`**, 441 событие, неожиданных 0; **Modes 0–29: 30 × `common = 1070, changed = 0, maxAbs = 0`**, +12 рядов |
| E3 | поведение | Mode 31: B производит стройматериалы (> 1.5 в день в окне), добывает реголит, строит генерацию, её CM fulfillment < 0.95, до всплеска производства нет; Mode 30: транспорт берёт из обеих колоний, доли в сумме ровно 1, fulfillment ≥ 0.98 |
| E4 | ревью патча | модель исполнителя **тождественна нашему skeleton**: 0 отличий в определениях, 0 лишних и недостающих элементов, одинаковые LINK и сценарии |
| E5 | продвижение | контракт §6 — см. ниже |

## Продвижение

- `orbital_economy_v7_7_1_r1_modeljson.json`, `validation-v7.7.1.json` (metadata-clean: только имена), `change-policy-v7.7.1-strict.json`; `reference/` → v7.7 r1; рабочая область стенда; манифесты.
- Аннотации +2 (switch и норма на мощность); реестр: 355 параметров, 189 аннотировано, 0 несимметричных без аннотации.
- Отчёты conformance / audit пересобраны (граничных 122 из 153, пар 15, closed-world 0, A/B 0).
- Новый эталон канонической платформы; документация; тег `v7.7.1-r1`.

## Замечание к исполнителю (без последствий)

Финальный коммит отчёта исполнитель пометил `[skip ci]` со ссылкой на нашу просьбу — такой просьбы не было; правило обратное (контракт §9.4 п. 1: дождаться зелёного CI). Вреда нет — коммит меняет только отчёт, и строгий guard при приёмке проверил голову ветки, — но это домысел, а не инструкция. В следующих заданиях: `[skip ci]` не использовать.

## Уроки

- **Цикл отлажен.** Две задачи по модели подряд — с первого раунда; модель исполнителя совпала со skeleton полностью (в 008 — с точностью до имён двух флагов). Самая долгая часть приёмки теперь — прогоны стенда, а не разбор расхождений.
- **Закрыт пробел v7.7:** путь стройматериалов колонии B впервые проверен на ненулевых значениях.
